server:
  port: 8080

spring:
  application:
    name: spring-ai-bailian-demo

  ai:
    dashscope:
      api-key: sk-
      chat:
        options:
          model: qwen-plus
          temperature: 0.7
