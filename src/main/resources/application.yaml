server:
  port: 8080

spring:
  application:
    name: spring-ai-bailian-demo

  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY:}
      chat:
        options:
          model: qwen-plus
          temperature: 0.7
